import { Text } from "react-native-paper";
import VStack from "../../components/Stacks/VStack";

const Menu = () => {
  return (
    <VStack style={{ alignItems: "center", justifyContent: "center", flex: 1 }}>
      <Text variant="titleMedium">Menu</Text>
    </VStack>
  );
};

export default Menu;
